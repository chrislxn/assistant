# 工程日志 2026-05-08

## 本次会话

类型：实施  
阶段：Phase 0.5 — M4 core-blocks 全线贯通  
参与：Chris + Claude Code (DeepSeek V4 Pro)

---

## 完成事项

M4 拆分为三个子任务完成：M4.1 (API)、M4.2 (migration)、M4.3 (bot 读取)。core_blocks 从纯数据库表转变为端到端可用的核心记忆层。

---

## M4.1：core-blocks CRUD API

**新增 3 个端点：**

- `GET /core-blocks` — 列出所有 active approved core blocks（block_key + version_no + content_text + metadata）
- `GET /core-blocks/{block_key}` — 查单个 active approved block，不存在返回 404
- `POST /core-blocks/{block_key}` — 创建新版本，调用 `create_core_block_version()`，不原地覆盖

**关键规则：**
- GET 只返回 `approval_status='approved' AND superseded_at IS NULL` 的最新版本
- POST 重复写同一 block_key → 旧版本 `superseded_at=NOW()`，新版本 `version_no+1`
- POST 默认 `auto_approve=True`（手动 API 调用直接审批）
- 端点加入 `PROTECTED_PREFIXES`（`/core-blocks`），受 `AdminAuthMiddleware` 保护
- 不改 startup 迁移、不改 get_persona、不改 context injection、不开放 AI/MCP

**文件改动：** `kiwi-mem/main.py` +106 行（3 端点 + 1 protected prefix）

---

## M4.2：__BOT_PERSONA__ 迁移到 core_blocks.response_policy

**迁移逻辑（`database.py` 新增 `migrate_persona_to_core_block()`）：**

1. 查询 core_blocks 是否已有 active `response_policy` → 有则跳过
2. 查询 memories 中 `title='__BOT_PERSONA__'` → 无则跳过
3. 调用 `create_core_block_version(block_key='response_policy', content_text=<persona>, proposed_by='migration', approved_by='migration', auto_approve=True)`
4. 将原 memory 标记 `status='archived'`, `memory_type='core_legacy'`，不删除
5. 任何异常 catch + warning，不阻止服务启动
6. 幂等：重启多次只产生 1 个 active block

**扩展 `create_core_block_version()`：** 新增 `approved_by` 参数（可选，默认 None），INSERT 时写入该列。

**调用点：** `main.py` `lifespan()` 中，在 `init_tables()` 和工厂 prompt 初始化之后执行。

**文件改动：**
- `kiwi-mem/database.py` +35 行（`migrate_persona_to_core_block` + `approved_by` 参数）
- `kiwi-mem/main.py` +7 行（import + lifespan 调用）

---

## M4.3：get_persona() 改为从 core_blocks 读取

**修改 `telegram-bot/bot.py` `get_persona()`：**

- 优先 `GET /core-blocks/response_policy`，200 + content_text 非空 → 直接用
- 404 或空内容 → `log.warning` + fallback 到旧 `GET /debug/memories?title=__BOT_PERSONA__`
- 异常时返回旧缓存（TTL 5 分钟）
- 不改 context injection、不改 MCP、不改 core-blocks API、不改其他记忆检索

**文件改动：** `telegram-bot/bot.py` ~15 行（get_persona 函数重写）

---

## 验证结果

### M4.1
| 步骤 | 结果 |
|------|------|
| POST test_block | version_no=1, approved ✅ |
| GET test_block | 返回 v1 ✅ |
| POST test_block 再写 | version_no=2, block_id=2 ✅ |
| GET test_block | 只返回 v2，旧版本已 superseded ✅ |
| SQL 查两版本 | v1: is_superseded=t, v2: is_superseded=f ✅ |
| GET /core-blocks | 列表正常 ✅ |
| 无 token 请求 | 401 拒绝 ✅ |

### M4.2
| 步骤 | 结果 |
|------|------|
| response_policy core block | version_no=1, approved, proposed_by/approved_by=migration ✅ |
| __BOT_PERSONA__ memory | status=archived, memory_type=core_legacy ✅ |
| 二次重启后 active block 数 | 1（幂等） ✅ |

### M4.3
| 步骤 | 结果 |
|------|------|
| core_blocks.response_policy 正常返回 | content 正确 ✅ |
| supersede 后 core_blocks 404 | fallback 到 memories 可用 ✅ |
| 恢复后 chat completions | 正常回复 ✅ |

---

## 涉及文件（实际改动）

| 文件 | 改动 |
|------|------|
| `kiwi-mem/main.py` | +113 行（M4.1 端点 + M4.2 lifespan 调用） |
| `kiwi-mem/database.py` | +42 行（approved_by 参数 + migrate_persona_to_core_block） |
| `telegram-bot/bot.py` | ~15 行（get_persona 优先读 core_blocks） |

---

## 下一步

5. **M5** — main.py：context injection 加入 core_blocks + log_memory_access
6. **M6** — mcp_server.py：source_trust 注入 + candidate 路径
7. 回归验证 + 验收标准检测

---

## 对应代码 commit

- **文档 repo** (`assistant-dev`): `bf14704` (Update project status and daily log)
- **实施 repo** (`~/assistant`): M4 代码改动待提交（`kiwi-mem/main.py`, `kiwi-mem/database.py`, `telegram-bot/bot.py` 已修改未 commit）